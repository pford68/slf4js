/**
 * Unit tests for slf4js
 */
const chai = require('chai');
const expect = chai.expect;
const sinon = require('sinon');

describe('slf4js', () => {

    describe('getLogger()', () => {

        beforeEach(() => {

        });

        afterEach(() => {

        });

        it('should work', () => {

        });

    });

});

